type Mutation {
    # Permet à un utilisateur inscrit de se connecter à son compte.
    login(email: String!, password: String!): Token!

    relog(token: String!): Token!
}

type Token {
    token: String!
}
